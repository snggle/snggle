import 'package:flutter/material.dart';

class PagesWrapper extends StatelessWidget {
  const PagesWrapper({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Hello Snuggle'),
    );
  }
}
